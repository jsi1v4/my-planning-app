import styled from 'styled-components';
import { Layout } from 'antd';

export const ContentCentered = styled(Layout.Content)`
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0.85;
`;

export default ContentCentered;
